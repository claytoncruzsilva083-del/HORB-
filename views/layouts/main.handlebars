<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if pageTitle}}{{pageTitle}}{{else}}HORB | Hotel Ouro Real Brilhante{{/if}}</title>
    <meta name="description" content="{{#if metaDescription}}{{metaDescription}}{{else}}Hotel Ouro Real Brilhante com estrutura premium, serviços completos e foco em experiência de alto padrão.{{/if}}">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="Hotel Ouro Real Brilhante">
    <meta property="og:url" content="{{#if pageUrl}}{{pageUrl}}{{else}}/{{/if}}">
    <meta property="og:title" content="{{#if pageTitle}}{{pageTitle}}{{else}}HORB | Hotel Ouro Real Brilhante{{/if}}">
    <meta property="og:description" content="{{#if metaDescription}}{{metaDescription}}{{else}}Hotel Ouro Real Brilhante com estrutura premium, serviços completos e foco em experiência de alto padrão.{{/if}}">
    <meta property="og:image" content="{{#if socialImage}}{{socialImage}}{{else}}/img/comida1.jpg{{/if}}">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{#if pageTitle}}{{pageTitle}}{{else}}HORB | Hotel Ouro Real Brilhante{{/if}}">
    <meta name="twitter:description" content="{{#if metaDescription}}{{metaDescription}}{{else}}Hotel Ouro Real Brilhante com estrutura premium, serviços completos e foco em experiência de alto padrão.{{/if}}">
    <meta name="twitter:image" content="{{#if socialImage}}{{socialImage}}{{else}}/img/comida1.jpg{{/if}}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;600;700&family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {{{body}}}
</body>
</html>